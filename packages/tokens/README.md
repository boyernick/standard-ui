# @standard-ui/tokens

CSS custom properties and Tailwind v4 `@theme` for Standard UI.

```css
@import "@standard-ui/tokens/css/tokens.css";
```

Set `--font-inter`, `--font-display`, and `--font-roboto-mono` on the document. Toggle dark with `class="dark"` on `<html>`.
